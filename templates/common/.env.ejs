PORT=5000
NODE_ENV=development
<% if (useMongoDb) { %>
MONGODB_URI=mongodb://localhost:27017/<%= projectName %>
<% } %><% if (useRedis) { %>
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
<% } %>
